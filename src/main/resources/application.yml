spring:
  datasource:
    driverClassName: org.postgresql.Driver
    username: postgres
    password: InnerJoin77
    url: jdbc:postgresql://localhost:5432/
  jpa:
    hibernate:
      ddl-auto: create
